# VoidWare — Vercel

Готовая версия сайта с серверной проверкой логина.

- `fucktuzguard.txt` и `timofeebtuzguardebeal.txt` находятся в `api/_private/` и не подключаются браузером.
- Браузер отправляет данные только на `/api/login`.
- `download.html` отсутствует в публичной части; версии находятся внутри личного кабинета.
- `vercel.json` уже содержит `includeFiles` для приватных TXT.

Важно: HTML/CSS/JS, которые браузер получил, нельзя сделать полностью невидимыми. Защищать нужно секреты и серверную логику.
